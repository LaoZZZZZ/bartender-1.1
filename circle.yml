dependencies:
  pre:
   - sudo apt-get update
   - sudo apt-get install build-essential g++ python-dev autotools-dev libicu-dev build-essential libbz2-dev 
   - sudo apt-get install libboost-all-dev
compile:
  override:
    - make all 
    - make clean;make all
        pwd:./src/test/test_code
test:
  override:
    - bash unit_test_runner.sh 
        pwd:./src/test/test_code
